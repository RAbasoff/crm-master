# -*- coding: utf-8 -*-
============================================================
СЕССИЯ CRM_Мастерская — 2026-09-26 (день)
============================================================

ДЕПЛОЙ: только функционал, данные не трогаем!

СТАТУС ДЕПЛОЯ: УСПЕШНО
Коммит: 94b18c6 → GitHub → PA (rabasoff.pythonanywhere.com)
Данные не трогались, миграция только CREATE TABLE IF NOT EXISTS

============================================================
ЧТО СДЕЛАНО
============================================================

1. ВНЕШНИЙ СКАНЕР (Honeywell + любой USB HID)
   - static/js/scanner-common.js — общий модуль
   - Автоперевод русской раскладки: Я→Z, Й→Q и т.д.
   - ПК: камера скрыта, работает только USB-сканер
   - Телефон: камера как раньше
   - Обновлены все страницы сканирования

2. ГАЗОВЫЕ БАЛЛОНЫ — восстановлен старый поток
   - Скан → новый баллон = FULL, второй автоматически = IN USE
   - Кнопка «＋ Install / Add» с выбором: Установить / В склад
   - Выбор: Скан или Вручную
   - Исправлен RU→Z при сканировании

3. TWO MERGE (объединение нарядов)
   - UI с чекбоксами + кнопка «Слияние» на списке TWO
   - Перенос: описание, рабочие, назначения, фото, подписи
   - Маршрут /two/merge + 4 маршрута consumables

4. ЗАПЧАСТИ СО СКЛАДА — ИСПРАВЛЕН 404
   - Добавлены маршруты: /consumables/link, /consume, /update-date, /unlink
   - Списание работает: остаток уменьшается + VoorraadMutatie

5. ЛОГОТИП
   - Форма входа: логотип + «CRM PRO systeem» (без текста ProMaster)
   - Печатные формы: TWO, календарь, склад, отчёты, статистика
   - Дашборд: логотип слева от названия
   - Favicon: logo.jpeg

6. НАПОМИНАНИЕ О ЗАМЕНЕ НОЖЕЙ
   - Жёлто-оранжевое окно, чёрный текст
   - Мигание 4 сек (8 циклов × 500мс), текст всегда чёрный
   - Кнопки: «ПОНЯЛ» (закрыть) + «ЗАМЕНИТЬ» (перейти)
   - «ЗАМЕНИТЬ» только для admin + tool_wear доступ
   - Показ при входе в модуль «Замена ножей» (для проверки)
   - Порог: 80% износа, исчезает после замены
   - Исправлен баг API: pre-filter отбрасывал инструменты

7. БЛОКИРОВКА РЕДАКТИРОВАНИЯ (admin priority)
   - RecordLock модель + миграция (CREATE TABLE IF NOT EXISTS)
   - acquire_lock / release_lock / refresh_lock в utils.py
   - Применено: machine_edit, warehouse_edit, two_edit
   - Admin может снять любую блокировку

8. АВТОВЫХОД ПО ПРОСТОЮ (30 мин, ПК, не admin)
   - JavaScript в base.html, отслеживает активность
   - Предупреждение за 5 мин до выхода
   - Телефон — без изменений

9. АВТОБЭКАП ПРИ ВХОДЕ
   - backup_database() вызывается при каждом входе
   - Сохраняет все изменения предыдущей сессии

10. УВЕДОМЛЕНИЯ: звук + вибрация
    - Двойной сигнал (880Hz + 1100Hz) при получении
    - Вибрация: [200, 100, 200, 100, 400] — как входящее сообщение

11. ГРАФИК ОБСЛУЖИВАНИЯ
    - Добавлено «Прочее оборудование» (free-text) в выбор цели

12. КАРТА ЦЕХА
    - Карта заполняет всё доступное пространство (85vh)
    - object-fit: contain — крупная
    - Показ прочего оборудования (⚡) с цветом статуса
    - Неисправные: пульсирующая анимация + красная подпись
    - Автозум при загрузке (zoomToFit)

13. ФОРМА ВХОДА
    - Логотип 140×140 с тенью
    - «CRM PRO systeem» под логотипом
    - Текст «A.B.A.S.O.F.F. ProMaster» убран

============================================================
БЕЗОПАСНОСТЬ ДАННЫХ
============================================================
- 27 файлов: только Python + HTML + CSS + JS
- Миграция БД: только CREATE TABLE IF NOT EXISTS record_lock
- НИ ОДНОГО DELETE / DROP / ALTER
- Автобэкап при входе создаётся автоматически

============================================================
ЗАДАЧИ НА ЗАВТРА
============================================================
1. Ежемесячный архив: склад (приход/расход), неисправности,
   ордера на работу, отчёты, статистика. Поиск по архиву.
2. Напоминания при входе по правам доступа (замена ножей →
   admin, director, мастера + ответственный)
3. Продолжить blueprint-рефакторинг app.py

============================================================

