============================================================
АВТОСОХРАНЕНИЕ СЕССИИ — ФИНАЛЬНОЕ
============================================================
Время: 2026-09-13 21:30 UTC

ПРОЕКТ: D:\ИИ\CRM_Мастерская
GITHUB: https://github.com/RAbasoff/crm-master.git
ONLINE: https://rabasoff.pythonanywhere.com (SQLite — ломается)

ПОСЛЕДНИЙ КОММИТ: a191e0f "feat: Railway.app deployment config + MySQL migration script"

============================================================
ЧТО СДЕЛАНО ЗА СЕССИЮ (16 коммитов):
============================================================

1. ИСПРАВЛЕНИЯ ОШИБОК:
   - flask_compress/caching/migrate сделаны опциональными
   - strict_slashes=False на /login (терял POST данные)
   - Login form явный action
   - CSRF flash message + WTF_CSRF_SSL_STRICT=False
   - 500 handler redirect для неавторизованных → /login
   - Login try/except + логирование в файл

2. ПОЛЬЗОВАТЕЛИ:
   - Удалены фейковые tech/user (Sergei Petrov, Jan de Vries)
   - Director может создавать TWO и удалять расписание

3. ГРАФИК РАБОТЫ:
   - Schedule показывает Monteurs (тех. служба), не Users
   - Director может удалять расписание
   - Monthly Schedule показывает Monteurs (auto User accounts)

4. LOGISTIEK-OKTOPUS:
   - Группа доступа ResponsibleGroup
   - WarehouseGroup для запчастей
   - Pablo (pablo/pablo123) и Paulina (paulina/paulina123)
   - Фильтрация: видят только свой склад
   - Low-stock notifications админу

5. КАЛЕНДАРЬ ТО:
   - Recurring events показываются после completion
   - Per-occurrence done tracking (MaintenanceRecord per date)

6. ПОДГОТОВКА К ПЕРЕНОСУ:
   - MySQL support в config.py
   - railway.json + Procfile для Railway.app
   - migrate_to_railway.py (экспорт SQLite → JSON → MySQL)
   - repair_db.py (ремонт повреждённой SQLite на PA)
   - create_logistiek.py (создание Logistiek группы)

============================================================
ЗАВТРАШНИЙ ПЛАН — ПЕРЕНОС НА RAILWAY.APP:
============================================================

1. Локально: python migrate_to_railway.py export
2. railway.app → New Project → MySQL → скопировать DATABASE_URL
3. railway.app → + New → GitHub Repo → RAbasoff/crm-master
4. Variables: DATABASE_URL + SECRET_KEY
5. Деплой автоматический
6. python migrate_to_railway.py import <DATABASE_URL>
7. python create_logistiek.py
8. Проверить сайт

============================================================
ПАРОЛИ:
   admin / Aba103sov
   director / director123
   pablo / pablo123
   paulina / paulina123
   (Monteur accounts создаются автоматически: имя.lower.123)

============================================================
